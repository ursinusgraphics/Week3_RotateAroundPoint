var mat3 = glMatrix.mat3;

/**
 * Construct a matrix that performs a rotation around a point
 * 
 * @param {float} theta Angle, in radians, of the rotation
 * @param {vec2} p Vector around which to rotate
 * @returns mat3 matrix for performing the rotation
 */
function getRotationAroundPoint(theta, p) {
    let m = mat3.create();
    return m;
}